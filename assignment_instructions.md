# โจทย์การบ้าน: Launch Day - เปิดตัวทีมพัฒนาเว็บไซต์

## 📝 สถานการณ์ (Scenario)
พวกคุณคือทีม Developer น้องใหม่ไฟแรง 3 คน ที่ตัดสินใจรวมตัวกันรับงาน Freelance ในชื่อทีมชั่วคราวว่า **"The Triangle Devs"** ลูกค้ารายแรกขอดู Portfolio ของทีมด่วนภายใน 24 ชั่วโมง! 

**ภารกิจ:** พวกคุณต้องช่วยกันสร้างหน้าเว็บไซต์แนะนำทีมแบบเร่งด่วน โดยใช้ **Git & GitHub** ในการจัดการไฟล์ เพื่อไม่ให้งานทับกันและดูเป็นมืออาชีพ

---

## 👥 บทบาทสมาชิก (Roles)
* **Student A (Team Lead):** ผู้เริ่มสร้าง Repo, ขึ้นโครงสร้างไฟล์, รวมงาน (Merge), และส่งงาน
* **Student B (Developer):** ผู้ร่วมพัฒนา, สร้าง Branch, แก้ไขส่วนของตัวเอง
* **Student C (Designer/Dev):** ผู้ร่วมพัฒนา, สร้าง Branch, แก้ไขส่วนของตัวเอง

---

## 📂 ไฟล์ที่ต้องส่ง (File Structure)
* `index.html` (หน้าแรก: เมนูหลัก, ประวัติทีม, รายชื่อสมาชิก)
* `portfolio.html` (ผลงานที่ผ่านมาของแต่ละคน)
* `about.html` (ข้อมูลส่วนตัวแบบเจาะลึกของแต่ละคน)

---

## 🚀 ขั้นตอนการทำงาน (Workflow)

### Phase 1: การเริ่มต้น (Setup) - โดย Student A
1.  สร้าง Folder โปรเจกต์
2.  ใช้ไฟล์ Template ที่ให้มา (index, portfolio, about)
3.  `git init` และ `git add .` และ `git commit` ครั้งแรก
4.  สร้าง Repository บน GitHub และ Push ขึ้นไป (`main`)
5.  ส่ง Link Repository ให้เพื่อน

### Phase 2: แยกย้ายกันทำงาน (Coding) - ทุกคนทำพร้อมกัน
1.  **Student B และ C:** ทำการ `git clone` ลงเครื่อง
2.  **ทุกคน (A, B, C):**
    * สร้าง Branch ส่วนตัว (เช่น `feature/student-a`)
    * **แก้ไขไฟล์ index.html:** เพิ่มชื่อ-รหัส ของตนเอง ในส่วน Team Members
    * **แก้ไขไฟล์ portfolio.html:** เพิ่มข้อมูลผลงานของตนเอง ใน Box ของตัวเอง
    * **แก้ไขไฟล์ about.html:** เพิ่มข้อมูลแนะนำตัว ในส่วนของตัวเอง
3.  ทำการ `git add`, `git commit`, และ `git push` Branch ของตัวเองขึ้น GitHub

### Phase 3: รวมร่าง (Merge) - ทีละคน
1.  **Student A:** ทำ Pull Request (PR) Branch ตัวเองเข้า `main` -> กด Merge
2.  **Student B:** ทำ PR เข้า `main` -> (ถ้ามี Conflict ให้แก้) -> กด Merge
3.  **Student C:** ทำ PR เข้า `main` -> (ถ้ามี Conflict ให้แก้) -> กด Merge

### Phase 4: ส่งงาน (Release) - โดย Student A
1.  กลับมาที่เครื่อง (`local`) สั่ง `git checkout main` และ `git pull` เพื่อเอาโค้ดล่าสุดของเพื่อนลงมา
2.  ตรวจสอบความถูกต้องของหน้าเว็บ
3.  สร้าง Tag: `git tag -a v1.0 -m "Final Submission"`
4.  Push Tag: `git push origin v1.0`
5.  ส่ง Link GitHub Repository ให้ผู้สอน

---

## ✅ Checklist การให้คะแนน
- [ ] มีการ Commit ที่สื่อความหมาย
- [ ] มี Branch ของสมาชิกครบทุกคน
- [ ] ไฟล์ HTML ทั้ง 3 ไฟล์มีข้อมูลสมาชิกครบ 3 คนถูกต้อง
- [ ] มีการทำ Pull Request และ Merge ครบถ้วน
- [ ] มี Tag v1.0 ปรากฏบน GitHub
